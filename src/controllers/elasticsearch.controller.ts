import { Request, Response } from "express";

export const createIndex = async(req: Request<Record<string,string>,void,void>, res:Response) => {
    try {
        
    } catch (error) {
        console.log("createIndex", error);
    }
}
